# 1. 의존성 (Dependencies)
# 용량이 크고 재설치가 가능하므로 제외
node_modules/
.pnp
.pnp.js

# 2. 빌드 결과물 (Production Builds)
# 소스 코드가 아닌 생성된 파일들이므로 제외
dist/
build/
out/
.next/

# 3. 환경 변수 및 비밀번호 (Security - 중요!)
# API 키나 DB 정보가 포함된 파일은 절대 공유 금지
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.local

# 4. Cursor AI & 에디터 설정 (IDE/Editor)
# Cursor AI 관련 캐시 및 설정 파일
.cursor/
.aiassistant/
.mcp*
CLAUDE.md
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# 5. 로그 및 캐시 (Logs & Caches)
# 개발 중 발생하는 임시 파일들
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.eslintcache
.stylelintcache
*.tsbuildinfo
.vite
.cache

# 6. 테스트 및 커버리지
coverage/